#!/bin/bash
apt-get update -y && apt-get install -y \
    curl \
    dnsutils \
    gdb \
    iputils-ping \
    ltrace \
    make \
    procps \
    strace \
    sudo \
    sysstat \
    unzip \
    nano \
    wget \
    bash